{
  "name": "comparison-table",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "sass": "sass --watch --no-source-map ./src:./build"
  },
  "devDependencies": {
    "sass": "^1.59.3",
    "vite": "^4.2.0"
  }
}
